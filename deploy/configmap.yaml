apiVersion: v1
kind: ConfigMap
metadata:
  name: rules-config
data:
  rules.yaml: |
    version: "1.0"
    rules:
      - name: block_bad_ip
        action: deny
        priority: 100
      - name: allow_internal
        action: allow
        priority: 10
